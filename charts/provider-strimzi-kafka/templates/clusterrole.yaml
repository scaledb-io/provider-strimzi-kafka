{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "provider-strimzi-kafka.fullname" . }}
  labels:
    {{- include "provider-strimzi-kafka.labels" . | nindent 4 }}
# Rules are generated by controller-gen from kubebuilder RBAC markers.
# Run `make generate` to update.
rules:
{{ .Files.Get "generated/rbac-rules.yaml" | nindent 2 }}
{{- end }}
