apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-strimzi-kafka.fullname" . }}
  labels:
    {{- include "provider-strimzi-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-strimzi-kafka.selectorLabels" . | nindent 4 }}
